<div class="rc-faq">
  <div class="rc-faq__container">
    {% if section.settings.heading %}
      <h2 class="rc-faq__heading">{{ section.settings.heading }}</h2>
    {% endif %}

    <div class="rc-faq__list">
      {% for block in section.blocks %}
        {% if block.type == "faq" %}
          <details class="rc-faq__item">
            <summary class="rc-faq__summary">
              <span class="rc-faq__question">{{ block.settings.question }}</span>
              <span class="rc-faq__icon"></span>
            </summary>
            <div class="rc-faq__answer">
              {{ block.settings.answer }}
            </div>
          </details>
        {% endif %}
      {% endfor %}
    </div>
  </div>
</div>

<style>
  .rc-faq {
    width: 100%;
    padding: var(--rc-section-md) 1.25rem;
    background-color: var(--rc-white);
  }

  .rc-faq__container {
    max-width: var(--rc-max-width);
    margin: 0 auto;
    padding: 0 var(--rc-page-padding);
  }

  .rc-faq__heading {
    font-family: var(--rc-font, 'DM Sans', sans-serif) !important;
    font-size: clamp(1.75rem, 3vw, 2.5rem) !important;
    font-weight: 500 !important;
    line-height: 120% !important;
    color: var(--rc-off-black, #1a1a1a) !important;
    margin: 0 0 3rem 0;
    text-align: center;
  }

  .rc-faq__list {
    display: flex;
    flex-direction: column;
  }

  .rc-faq__item {
    border-bottom: 1px solid var(--rc-off-black, #1a1a1a) !important;
    opacity: 1;
  }

  .rc-faq__item:last-child {
    border-bottom: none;
  }

  .rc-faq__summary {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 1.5rem 0;
    cursor: pointer;
    list-style: none;
    user-select: none;
    font-family: var(--rc-font);
  }

  .rc-faq__summary::-webkit-details-marker {
    display: none;
  }

  .rc-faq__question {
    font-family: var(--rc-font, 'DM Sans', sans-serif) !important;
    font-size: 1.125rem !important;
    font-weight: 700 !important;
    color: var(--rc-off-black, #1a1a1a) !important;
    flex: 1;
    padding-right: 1.25rem;
    text-align: left;
  }

  .rc-faq__icon {
    width: 1.25rem;
    height: 1.25rem;
    flex-shrink: 0;
    position: relative;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  /* Plus icon created with CSS */
  .rc-faq__icon::before {
    content: '';
    width: 1rem;
    height: 0.125rem;
    background-color: var(--rc-off-black, #1a1a1a);
    position: absolute;
  }

  .rc-faq__icon::after {
    content: '';
    width: 0.125rem;
    height: 1rem;
    background-color: var(--rc-off-black, #1a1a1a);
    position: absolute;
  }

  /* Rotate minus on open */
  details[open] .rc-faq__icon::after {
    height: 0;
  }

  details[open] .rc-faq__icon::before {
    transform: rotate(0deg);
  }

  .rc-faq__answer {
    padding: 0 0 1.5rem 0;
    font-family: var(--rc-font, 'DM Sans', sans-serif) !important;
    font-size: 1.125rem !important;
    font-weight: 400 !important;
    line-height: 1.6 !important;
    color: var(--rc-off-black, #1a1a1a) !important;
    text-transform: none !important;
    animation: slideDown 0.3s ease-out;
  }

  @keyframes slideDown {
    from {
      opacity: 0;
      transform: translateY(-0.625rem);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  @media (max-width: 768px) {
    .rc-faq {
      padding: var(--rc-section-md) 1.25rem;
    }

    .rc-faq__heading {
      margin-bottom: 2.5rem;
    }

    .rc-faq__summary {
      padding: 1.25rem 0;
    }

    .rc-faq__question {
      font-size: 1rem !important;
      padding-right: 1rem;
    }

    .rc-faq__answer {
      font-size: 1rem !important;
      padding-bottom: 1.25rem;
    }

    .rc-faq__container {
      padding: 0 1.25rem;
    }
  }
</style>

{% schema %}
{
  "name": "RC FAQ",
  "settings": [
    {
      "type": "text",
      "id": "heading",
      "label": "Section Heading",
      "placeholder": "e.g., Frequently Asked Questions"
    }
  ],
  "blocks": [
    {
      "type": "faq",
      "name": "FAQ Item",
      "settings": [
        {
          "type": "text",
          "id": "question",
          "label": "Question",
          "placeholder": "Enter the question"
        },
        {
          "type": "textarea",
          "id": "answer",
          "label": "Answer",
          "placeholder": "Enter the answer"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "RC FAQ"
    }
  ]
}
{% endschema %}
