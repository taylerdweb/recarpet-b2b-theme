{% comment %}
  reCarpet Om oss — comprehensive page with 9 distinct sections
  A. Intro, B. Founder Quote, C. Team, D. Stats, E. Projects Portfolio,
  F. Expertise Quote, G. Services Summary, H. Vision, I. Sustainable Solution
{% endcomment %}

<style>
  /* ===== Team Section — text left, image right ===== */
  .rc-about-team {
    background: var(--rc-white);
    padding: var(--rc-section-md) 0;
  }
  .rc-about-team__inner {
    max-width: var(--rc-max-width);
    margin: 0 auto;
    padding: 0 var(--rc-page-padding);
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 4rem;
    align-items: start;
  }
  .rc-about-team__text {
    display: flex;
    flex-direction: column;
  }
  .rc-team-member--spaced {
    margin-bottom: 2.5rem;
  }
  .rc-team-member__name {
    font-family: var(--rc-font, 'DM Sans', sans-serif) !important;
    font-size: 1.125rem !important;
    color: var(--rc-off-black, #1a1a1a) !important;
    margin: 0 0 0.5rem !important;
    line-height: 1.5 !important;
  }
  .rc-team-member__name strong {
    font-weight: 700 !important;
  }
  .rc-team-member__bio {
    font-family: var(--rc-font, 'DM Sans', sans-serif) !important;
    font-size: 1.125rem !important;
    line-height: 1.65 !important;
    color: var(--rc-off-black, #1a1a1a) !important;
    margin: 0 0 0.5rem !important;
    text-transform: none !important;
  }
  .rc-team-member__email {
    color: var(--rc-off-black, #1a1a1a) !important;
    text-decoration: underline;
  }
  .rc-team-member__email:hover {
    color: var(--rc-green, #1e4433) !important;
  }
  .rc-about-team__image {
    border-radius: var(--rc-radius);
    overflow: hidden;
  }
  .rc-about-team__image img {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  /* ===== D. Stats Row ===== */
  .rc-about-stats {
    background: var(--rc-cream);
    padding: var(--rc-section-sm) 0;
  }
  .rc-about-stats__inner {
    max-width: var(--rc-max-width);
    margin: 0 auto;
    padding: 0 var(--rc-page-padding);
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 2rem;
    text-align: center;
  }
  .rc-stat__number {
    font-size: clamp(2.25rem, 4vw, var(--rc-h1));
    font-weight: 700;
    color: var(--rc-green);
    line-height: 1;
    margin-bottom: 0.375rem;
  }
  .rc-stat__label {
    font-size: var(--rc-text-sm);
    color: var(--rc-text-muted);
    line-height: 1.4;
  }

  /* ===== E. Projects Portfolio ===== */
  .rc-about-projects {
    background: var(--rc-white);
    padding: var(--rc-section-md) 0;
  }
  .rc-about-projects__inner {
    max-width: var(--rc-max-width);
    margin: 0 auto;
    padding: 0 var(--rc-page-padding);
  }
  .rc-about-projects__heading {
    font-size: clamp(1.75rem, 3vw, var(--rc-h3));
    font-weight: 500;
    color: var(--rc-text);
    margin: 0 0 3rem;
    text-align: center;
  }
  .rc-about-projects__grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 3rem;
  }
  .rc-project-category__title {
    font-size: var(--rc-text-md);
    font-weight: 600;
    color: var(--rc-text);
    margin: 0 0 1.25rem;
  }
  .rc-project-category__list {
    list-style: none;
    padding: 0;
    margin: 0;
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
  }
  .rc-project-category__item {
    font-size: var(--rc-text-sm);
    color: var(--rc-text-muted);
    line-height: 1.6;
  }

  /* ===== F. Expertise Quote ===== */
  .rc-about-expertise {
    background: var(--rc-white);
    padding: var(--rc-section-md) 0;
  }
  .rc-about-expertise__inner {
    max-width: var(--rc-max-width);
    margin: 0 auto;
    padding: 0 var(--rc-page-padding);
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 4rem;
    align-items: center;
  }
  .rc-about-expertise__body {
    font-size: var(--rc-text-base);
    line-height: 1.75;
    color: var(--rc-text-muted);
    margin: 0 0 1.5rem;
  }
  .rc-about-expertise__quote {
    font-size: var(--rc-h4);
    font-weight: 500;
    line-height: 1.4;
    color: var(--rc-text);
    font-style: italic;
    margin: 0;
  }

  /* ===== G. Services Summary ===== */
  .rc-about-services {
    background: var(--rc-cream);
    padding: var(--rc-section-md) 0;
  }
  .rc-about-services__inner {
    max-width: var(--rc-max-width);
    margin: 0 auto;
    padding: 0 var(--rc-page-padding);
  }
  .rc-about-services__heading {
    font-size: clamp(1.75rem, 3vw, var(--rc-h3));
    font-weight: 500;
    color: var(--rc-text);
    margin: 0 0 2rem;
  }
  .rc-about-services__list {
    list-style: none;
    padding: 0;
    margin: 0 0 2rem;
    display: flex;
    flex-direction: column;
    gap: 0.75rem;
  }
  .rc-about-services__item {
    font-size: var(--rc-text-base);
    color: var(--rc-text-muted);
    line-height: 1.6;
    padding-left: 1.5rem;
    position: relative;
  }
  .rc-about-services__item::before {
    content: '→';
    position: absolute;
    left: 0;
    color: var(--rc-green);
    font-weight: 600;
  }
  .rc-about-services__buttons {
    display: flex;
    gap: 1rem;
    flex-wrap: wrap;
  }

  /* ===== H. Vision Section ===== */
  .rc-about-vision {
    background: var(--rc-white);
    padding: var(--rc-section-md) 0;
  }
  .rc-about-vision__inner {
    max-width: var(--rc-max-width);
    margin: 0 auto;
    padding: 0 var(--rc-page-padding);
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 4rem;
    align-items: center;
  }
  .rc-about-vision__heading {
    font-size: clamp(1.75rem, 3vw, var(--rc-h3));
    font-weight: 500;
    color: var(--rc-text);
    margin: 0 0 1.25rem;
  }
  .rc-about-vision__body {
    font-size: var(--rc-text-base);
    line-height: 1.75;
    color: var(--rc-text-muted);
    margin: 0;
  }
  .rc-about-vision__image {
    border-radius: var(--rc-radius);
    overflow: hidden;
    aspect-ratio: 4/3;
  }
  .rc-about-vision__image img {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  /* ===== I. Sustainable Solution ===== */
  .rc-about-sustainability {
    background: var(--rc-white);
    padding: var(--rc-section-md) 0;
  }
  .rc-about-sustainability__inner {
    max-width: var(--rc-max-width);
    margin: 0 auto;
    padding: 0 var(--rc-page-padding);
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 4rem;
    align-items: center;
  }
  .rc-about-sustainability__image {
    border-radius: var(--rc-radius);
    overflow: hidden;
    aspect-ratio: 4/3;
  }
  .rc-about-sustainability__image img {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }
  .rc-about-sustainability__heading {
    font-size: clamp(1.75rem, 3vw, var(--rc-h3));
    font-weight: 500;
    color: var(--rc-text);
    margin: 0 0 0.5rem;
  }
  .rc-about-sustainability__subtitle {
    font-size: var(--rc-text-base);
    font-weight: 500;
    color: var(--rc-green);
    margin: 0 0 1.5rem;
  }
  .rc-about-sustainability__body {
    font-size: var(--rc-text-base);
    line-height: 1.75;
    color: var(--rc-text-muted);
    margin: 0 0 1.5rem;
  }
  .rc-about-sustainability__body p {
    margin: 0 0 1rem;
  }
  .rc-about-sustainability__body p:last-child {
    margin: 0;
  }

  /* ===== Responsive ===== */
  @media (max-width: 900px) {
    .rc-about-team__inner,
    .rc-about-expertise__inner,
    .rc-about-vision__inner,
    .rc-about-sustainability__inner {
      grid-template-columns: 1fr;
      gap: 2rem;
    }
    .rc-about-projects__grid {
      grid-template-columns: 1fr;
    }
    .rc-about-stats__inner {
      grid-template-columns: repeat(2, 1fr);
    }
    .rc-about-stats__inner,
    .rc-about-team__inner,
    .rc-about-projects__inner,
    .rc-about-expertise__inner,
    .rc-about-services__inner,
    .rc-about-vision__inner,
    .rc-about-sustainability__inner {
      padding: 0 1.25rem;
    }
  }

  @media (max-width: 480px) {
    .rc-about-stats__inner {
      grid-template-columns: 1fr;
    }
    .rc-about-services__buttons {
      flex-direction: column;
    }
  }
</style>

<!-- Team Section — text left, image right -->
{%- assign team_blocks = section.blocks | where: "type", "team_member" -%}
{%- if team_blocks.size > 0 -%}
  <section class="rc-about-team">
    <div class="rc-about-team__inner">
      <div class="rc-about-team__text">
        {%- for block in section.blocks -%}
          {%- if block.type == 'team_member' -%}
            <div class="rc-team-member{% unless forloop.last %} rc-team-member--spaced{% endunless %}">
              <p class="rc-team-member__name"><strong>{{ block.settings.name }} – {{ block.settings.title }}</strong></p>
              <p class="rc-team-member__bio">{{ block.settings.bio }}</p>
              {%- if block.settings.email != blank -%}
                <p class="rc-team-member__bio">Kontakta {{ block.settings.name | split: ' ' | first }} – <a href="mailto:{{ block.settings.email }}" class="rc-team-member__email">{{ block.settings.email }}</a></p>
              {%- endif -%}
            </div>
          {%- endif -%}
        {%- endfor -%}
      </div>
      <div class="rc-about-team__image">
        {%- if section.settings.team_image != blank -%}
          <img src="{{ section.settings.team_image | image_url: width: 900 }}" alt="Vårt team" loading="lazy">
        {%- else -%}
          <div style="background:var(--rc-cream-alt, #E6E3DE);height:100%;min-height:20rem;"></div>
        {%- endif -%}
      </div>
    </div>
  </section>
{%- endif -%}

<!-- D. Stats Row -->
{%- assign stat_blocks = section.blocks | where: "type", "stat" -%}
{%- if stat_blocks.size > 0 -%}
  <section class="rc-about-stats">
    <div class="rc-about-stats__inner">
      {%- for block in section.blocks -%}
        {%- if block.type == 'stat' -%}
          <div class="rc-stat">
            <div class="rc-stat__number">{{ block.settings.number }}</div>
            <div class="rc-stat__label">{{ block.settings.label }}</div>
          </div>
        {%- endif -%}
      {%- endfor -%}
    </div>
  </section>
{%- endif -%}

<!-- E. Projects Portfolio -->
{%- assign project_blocks = section.blocks | where: "type", "project_category" -%}
{%- if project_blocks.size > 0 -%}
  <section class="rc-about-projects">
    <div class="rc-about-projects__inner">
      {%- if section.settings.projects_heading != blank -%}
        <h2 class="rc-about-projects__heading">{{ section.settings.projects_heading }}</h2>
      {%- endif -%}
      <div class="rc-about-projects__grid">
        {%- for block in section.blocks -%}
          {%- if block.type == 'project_category' -%}
            <div>
              <h3 class="rc-project-category__title">{{ block.settings.title }}</h3>
              {%- if block.settings.items != blank -%}
                <ul class="rc-project-category__list">
                  {%- assign items = block.settings.items | newline_to_br | split: '<br />' -%}
                  {%- for item in items -%}
                    {%- if item != blank -%}
                      <li class="rc-project-category__item">{{ item | strip }}</li>
                    {%- endif -%}
                  {%- endfor -%}
                </ul>
              {%- endif -%}
            </div>
          {%- endif -%}
        {%- endfor -%}
      </div>
    </div>
  </section>
{%- endif -%}

<!-- F. Expertise Quote Section -->
{%- if section.settings.expertise_body != blank or section.settings.expertise_quote != blank -%}
  <section class="rc-about-expertise">
    <div class="rc-about-expertise__inner">
      <div>
        {%- if section.settings.expertise_body != blank -%}
          <p class="rc-about-expertise__body">{{ section.settings.expertise_body }}</p>
        {%- endif -%}
      </div>
      <div>
        {%- if section.settings.expertise_quote != blank -%}
          <blockquote class="rc-about-expertise__quote">{{ section.settings.expertise_quote }}</blockquote>
        {%- endif -%}
      </div>
    </div>
  </section>
{%- endif -%}

<!-- G. Services Summary Section -->
{%- if section.settings.services_heading != blank -%}
  <section class="rc-about-services">
    <div class="rc-about-services__inner">
      <h2 class="rc-about-services__heading">{{ section.settings.services_heading }}</h2>
      {%- if section.settings.services_list != blank -%}
        <ul class="rc-about-services__list">
          {%- assign services = section.settings.services_list | newline_to_br | split: '<br />' -%}
          {%- for service in services -%}
            {%- if service != blank -%}
              <li class="rc-about-services__item">{{ service | strip }}</li>
            {%- endif -%}
          {%- endfor -%}
        </ul>
      {%- endif -%}
      <div class="rc-about-services__buttons">
        {%- if section.settings.services_btn1_label != blank -%}
          <a href="{{ section.settings.services_btn1_url }}" class="rc-btn rc-btn--primary">{{ section.settings.services_btn1_label }}</a>
        {%- endif -%}
        {%- if section.settings.services_btn2_label != blank -%}
          <a href="{{ section.settings.services_btn2_url }}" class="rc-btn rc-btn--secondary">{{ section.settings.services_btn2_label }}</a>
        {%- endif -%}
      </div>
    </div>
  </section>
{%- endif -%}

<!-- H. Vision Section -->
{%- if section.settings.vision_heading != blank or section.settings.vision_body != blank -%}
  <section class="rc-about-vision">
    <div class="rc-about-vision__inner">
      <div>
        <h2 class="rc-about-vision__heading">{{ section.settings.vision_heading }}</h2>
        <p class="rc-about-vision__body">{{ section.settings.vision_body }}</p>
      </div>
      {%- if section.settings.vision_image != blank -%}
        <div class="rc-about-vision__image">
          <img src="{{ section.settings.vision_image | image_url: width: 900 }}" alt="{{ section.settings.vision_heading }}" loading="lazy">
        </div>
      {%- endif -%}
    </div>
  </section>
{%- endif -%}

<!-- I. Sustainable Solution Section -->
{%- if section.settings.sustainability_heading != blank -%}
  <section class="rc-about-sustainability">
    <div class="rc-about-sustainability__inner">
      {%- if section.settings.sustainability_image != blank -%}
        <div class="rc-about-sustainability__image">
          <img src="{{ section.settings.sustainability_image | image_url: width: 900 }}" alt="{{ section.settings.sustainability_heading }}" loading="lazy">
        </div>
      {%- endif -%}
      <div>
        <h2 class="rc-about-sustainability__heading">{{ section.settings.sustainability_heading }}</h2>
        {%- if section.settings.sustainability_subtitle != blank -%}
          <p class="rc-about-sustainability__subtitle">{{ section.settings.sustainability_subtitle }}</p>
        {%- endif -%}
        {%- if section.settings.sustainability_body != blank -%}
          <div class="rc-about-sustainability__body">
            {{ section.settings.sustainability_body | newline_to_br }}
          </div>
        {%- endif -%}
        {%- if section.settings.sustainability_btn_label != blank -%}
          <a href="{{ section.settings.sustainability_btn_url }}" class="rc-btn rc-btn--primary">{{ section.settings.sustainability_btn_label }}</a>
        {%- endif -%}
      </div>
    </div>
  </section>
{%- endif -%}

{% schema %}
{
  "name": "reCarpet Om oss",
  "tag": "div",
  "class": "section",
  "settings": [
    {
      "type": "header",
      "content": "Team"
    },
    {
      "type": "image_picker",
      "id": "team_image",
      "label": "Teambild (höger kolumn)"
    },
    {
      "type": "header",
      "content": "E. Projects Portfolio"
    },
    {
      "type": "text",
      "id": "projects_heading",
      "label": "Rubrik",
      "default": "Några av våra lyckade projekt"
    },
    {
      "type": "header",
      "content": "F. Expertise Quote"
    },
    {
      "type": "textarea",
      "id": "expertise_body",
      "label": "Brödtext"
    },
    {
      "type": "textarea",
      "id": "expertise_quote",
      "label": "Citat"
    },
    {
      "type": "header",
      "content": "G. Services Summary"
    },
    {
      "type": "text",
      "id": "services_heading",
      "label": "Rubrik"
    },
    {
      "type": "textarea",
      "id": "services_list",
      "label": "Lista (en rad per punkt)"
    },
    {
      "type": "text",
      "id": "services_btn1_label",
      "label": "Knapp 1 text"
    },
    {
      "type": "url",
      "id": "services_btn1_url",
      "label": "Knapp 1 länk"
    },
    {
      "type": "text",
      "id": "services_btn2_label",
      "label": "Knapp 2 text"
    },
    {
      "type": "url",
      "id": "services_btn2_url",
      "label": "Knapp 2 länk"
    },
    {
      "type": "header",
      "content": "H. Vision"
    },
    {
      "type": "text",
      "id": "vision_heading",
      "label": "Rubrik"
    },
    {
      "type": "textarea",
      "id": "vision_body",
      "label": "Text"
    },
    {
      "type": "image_picker",
      "id": "vision_image",
      "label": "Bild"
    },
    {
      "type": "header",
      "content": "I. Sustainable Solution"
    },
    {
      "type": "text",
      "id": "sustainability_heading",
      "label": "Rubrik"
    },
    {
      "type": "text",
      "id": "sustainability_subtitle",
      "label": "Underrubrik"
    },
    {
      "type": "textarea",
      "id": "sustainability_body",
      "label": "Text (flerrader)"
    },
    {
      "type": "image_picker",
      "id": "sustainability_image",
      "label": "Bild"
    },
    {
      "type": "text",
      "id": "sustainability_btn_label",
      "label": "Knapptext"
    },
    {
      "type": "url",
      "id": "sustainability_btn_url",
      "label": "Knapp länk"
    }
  ],
  "blocks": [
    {
      "type": "stat",
      "name": "Statistik",
      "settings": [
        {
          "type": "text",
          "id": "number",
          "label": "Nummer",
          "default": "0"
        },
        {
          "type": "text",
          "id": "label",
          "label": "Etikett",
          "default": "Label"
        }
      ]
    },
    {
      "type": "team_member",
      "name": "Teammedlem",
      "settings": [
        {
          "type": "image_picker",
          "id": "image",
          "label": "Foto"
        },
        {
          "type": "text",
          "id": "name",
          "label": "Namn",
          "default": "Namn"
        },
        {
          "type": "text",
          "id": "title",
          "label": "Titel",
          "default": "Titel"
        },
        {
          "type": "textarea",
          "id": "bio",
          "label": "Bio"
        },
        {
          "type": "text",
          "id": "email",
          "label": "E-post"
        },
        {
          "type": "text",
          "id": "phone",
          "label": "Telefon"
        }
      ]
    },
    {
      "type": "project_category",
      "name": "Projektkategori",
      "settings": [
        {
          "type": "text",
          "id": "title",
          "label": "Kategori",
          "default": "Kategori"
        },
        {
          "type": "textarea",
          "id": "items",
          "label": "Projekt (en rad per projekt)"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "reCarpet Om oss",
      "settings": {
        "projects_heading": "Några av våra lyckade projekt",
        "vision_heading": "Vår vision",
        "sustainability_heading": "En hållbar lösning"
      }
    }
  ]
}
{% endschema %}