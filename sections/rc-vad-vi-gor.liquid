{% comment %}
  reCarpet Vad vi gör — Services overview + detail sections
  Used on /pages/vad-vi-gor (Tjänster page)
{% endcomment %}

<style>
  /* ===== Timeline navigation ===== */
  .rc-timeline-nav {
    background: var(--rc-cream, #f5f3ef);
    padding: var(--rc-section-md) var(--rc-page-padding);
  }
  .rc-timeline-nav__inner {
    max-width: var(--rc-max-width);
    margin: 0 auto;
  }
  .rc-timeline-nav__track {
    display: grid;
    grid-template-columns: repeat({{ section.blocks.size }}, 1fr);
    grid-template-rows: auto 2rem auto;
    position: relative;
  }
  /* Horizontal connecting line — fades out at the right */
  .rc-timeline-nav__track::before {
    content: '';
    grid-row: 2;
    grid-column: 1 / -1;
    height: 3px;
    background: linear-gradient(to right, var(--rc-off-black, #1a1a1a) 80%, transparent 100%);
    align-self: center;
    z-index: 1;
  }
  /* Top text (odd items: 1,3,5) */
  .rc-timeline-nav__text--top {
    grid-row: 1;
    align-self: end;
    padding-bottom: 1.25rem;
  }
  /* Bottom text (even items: 2,4,6) */
  .rc-timeline-nav__text--bottom {
    grid-row: 3;
    align-self: start;
    padding-top: 1.25rem;
  }
  .rc-timeline-nav__link {
    text-decoration: none;
    display: block;
  }
  .rc-timeline-nav__link:hover .rc-timeline-nav__title {
    color: var(--rc-green, #1e4433);
  }
  .rc-timeline-nav__title {
    font-family: var(--rc-font);
    font-size: 1.5rem;
    font-weight: 600;
    color: var(--rc-off-black, #1a1a1a);
    margin-bottom: 0.375rem;
    line-height: 1.3;
    transition: color 0.2s;
  }
  .rc-timeline-nav__desc {
    font-family: var(--rc-font);
    font-size: 1rem;
    color: var(--rc-text-muted, #666);
    line-height: 1.5;
    max-width: 15rem;
  }
  /* Dot cells — dot rendered via ::before to avoid Dawn anchor overrides */
  .rc-timeline-nav__dot-cell {
    grid-row: 2;
    display: flex;
    align-items: center;
    position: relative;
    z-index: 2;
    cursor: pointer;
  }
  .rc-timeline-nav__dot-cell::before {
    content: '' !important;
    width: 15px !important;
    height: 15px !important;
    min-width: 15px;
    min-height: 15px;
    border-radius: 50% !important;
    background: #8ba99a !important;
    box-shadow: 0 0 0 7px var(--rc-cream, #f5f3ef);
    display: block !important;
    flex-shrink: 0;
    transition: transform 0.15s;
  }
  .rc-timeline-nav__dot-cell:hover::before {
    transform: scale(1.3);
  }
  /* Hide the anchor visually — keep for accessibility */
  .rc-timeline-nav__dot {
    position: absolute;
    inset: 0;
    opacity: 0;
  }

  /* ===== Service detail section ===== */
  .rc-service-detail {
    padding: var(--rc-section-md) 0;
    background: var(--rc-cream, #f5f3ef);
  }
  .rc-service-detail__inner {
    max-width: var(--rc-max-width);
    margin: 0 auto;
    padding: 0 var(--rc-page-padding);
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 5.5rem;
    align-items: center;
  }
  .rc-service-detail__eyebrow {
    font-family: var(--rc-font) !important;
    font-size: var(--rc-text-sm, 0.875rem) !important;
    font-weight: 600 !important;
    color: var(--rc-off-black, #1a1a1a) !important;
    margin-bottom: 0.5rem;
    display: inline-block;
    line-height: var(--rc-body-lh, 1.5);
  }
  .rc-service-detail__heading {
    font-size: clamp(1.5rem, 3vw, var(--rc-h3));
    font-weight: 500;
    color: var(--rc-text);
    margin: 0 0 1.5rem;
  }
  .rc-service-detail__body {
    font-family: var(--rc-font, 'DM Sans', sans-serif) !important;
    font-size: 1rem !important;
    font-weight: 400 !important;
    line-height: 1.6 !important;
    color: var(--rc-text-muted) !important;
    margin: 1rem 0 0.75rem;
  }
  .rc-service-detail__list {
    list-style: none;
    padding: 0;
    margin: 1rem 0 1.5rem;
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
  }
  .rc-service-detail .rc-service-detail__list li {
    font-family: var(--rc-font, 'DM Sans', sans-serif) !important;
    font-size: 1rem !important;
    font-weight: 400 !important;
    line-height: 1.6 !important;
    color: var(--rc-text-muted) !important;
    padding-left: 1.75rem;
    position: relative;
  }
  .rc-service-detail__list li::before {
    content: '';
    position: absolute;
    left: 0;
    top: 0.25em;
    width: 16px;
    height: 17px;
    background-image: url("data:image/svg+xml,%3Csvg width='16' height='17' viewBox='0 0 16 17' fill='none' xmlns='http://www.w3.org/2000/svg'%3E%3Cpath d='M7.52867 12.0288L8.47133 12.9715L12.9427 8.50014L8.47133 4.02881L7.52867 4.97147L10.3907 7.83348H4V9.16681H10.3907L7.52867 12.0288Z' fill='%231E4433'/%3E%3C/svg%3E");
    background-repeat: no-repeat;
    background-size: contain;
  }
  .rc-service-detail__body-after {
    font-family: var(--rc-font, 'DM Sans', sans-serif) !important;
    font-size: 1rem !important;
    font-weight: 400 !important;
    line-height: 1.6 !important;
    color: var(--rc-text-muted) !important;
    margin: 0 0 1.5rem;
  }
  .rc-service-detail__image { border-radius: var(--rc-radius); overflow: hidden; aspect-ratio: 4/3; }
  .rc-service-detail__image img { width: 100%; height: 100%; object-fit: cover; }

  @media (max-width: 900px) {
    /* ===== Timeline mobile — vertical with dots & line ===== */
    .rc-timeline-nav {
      padding: 2.5rem 1.25rem;
    }
    .rc-timeline-nav__track {
      display: flex !important;
      flex-direction: column;
      gap: 0;
      position: relative;
      padding-left: 2.75rem;
    }
    /* Vertical connecting line — black, height set by JS to stop at last dot */
    .rc-timeline-nav__track::before {
      content: '' !important;
      position: absolute !important;
      left: 6px;
      top: 0.45rem;
      bottom: auto !important;
      width: 3px !important;
      height: var(--rc-timeline-line-height, 100%) !important;
      background: var(--rc-off-black, #1a1a1a) !important;
      border-radius: 2px;
      grid-row: unset;
      grid-column: unset;
      align-self: unset;
      z-index: 1;
    }
    /* Hide the separate dot cells — dots are on text items instead */
    .rc-timeline-nav__dot-cell {
      display: none !important;
    }
    /* Text items become timeline entries */
    .rc-timeline-nav__text--top,
    .rc-timeline-nav__text--bottom {
      padding: 0 0 2rem 0 !important;
      border-bottom: none;
      position: relative;
      grid-row: unset;
      align-self: unset;
    }
    /* Dot on each text item via ::before */
    .rc-timeline-nav__text--top::before,
    .rc-timeline-nav__text--bottom::before {
      content: '' !important;
      display: block !important;
      position: absolute;
      left: -2.75rem;
      top: 0.2rem;
      width: 15px;
      height: 15px;
      background: #8ba99a !important;
      border-radius: 50% !important;
      box-shadow: 0 0 0 5px var(--rc-cream, #f5f3ef);
      z-index: 2;
    }
    .rc-timeline-nav__title {
      font-size: 1.25rem;
    }
    .rc-timeline-nav__desc {
      max-width: none;
    }

    /* Service detail mobile */
    .rc-service-detail__inner { grid-template-columns: 1fr; gap: 2rem; }
    .rc-service-detail__inner { padding: 0 1.25rem; }
  }
</style>

<script>
  document.addEventListener('DOMContentLoaded', function() {
    // Smooth scroll when clicking timeline links
    document.querySelectorAll('.rc-timeline-nav__link, .rc-timeline-nav__dot').forEach(function(el) {
      el.addEventListener('click', function(e) {
        var anchor = this.getAttribute('href');
        if (anchor && anchor.startsWith('#')) {
          e.preventDefault();
          var target = document.querySelector('[data-service-section="' + anchor.substring(1) + '"]');
          if (target) {
            target.scrollIntoView({ behavior: 'smooth', block: 'start' });
          }
        }
      });
    });

    // Mobile: trim the timeline line to stop at the last dot
    if (window.innerWidth <= 900) {
      var track = document.querySelector('.rc-timeline-nav__track');
      if (track) {
        var textItems = track.querySelectorAll('[class*="rc-timeline-nav__text"]');
        var lastText = textItems[textItems.length - 1];
        if (lastText) {
          // Line should end at the last dot's center (0.45rem from top of last item)
          var trackRect = track.getBoundingClientRect();
          var lastRect = lastText.getBoundingClientRect();
          var lineEnd = lastRect.top - trackRect.top + 11; // 11px ≈ dot center (0.2rem + 7.5px)
          track.style.setProperty('--rc-timeline-line-height', lineEnd + 'px');
        }
      }
    }
  });
</script>

<!-- Timeline navigation -->
<nav class="rc-timeline-nav" aria-label="Tjänster">
  <div class="rc-timeline-nav__inner">
    <div class="rc-timeline-nav__track">
      {%- for block in section.blocks -%}
        {%- assign mod = forloop.index | modulo: 2 -%}
        <div class="rc-timeline-nav__text {% if mod == 1 %}rc-timeline-nav__text--top{% else %}rc-timeline-nav__text--bottom{% endif %}" style="grid-column: {{ forloop.index }}">
          <a href="#{{ block.settings.anchor }}" class="rc-timeline-nav__link">
            <div class="rc-timeline-nav__title">{{ block.settings.eyebrow }}</div>
            {%- if block.settings.nav_desc != blank -%}
              <div class="rc-timeline-nav__desc">{{ block.settings.nav_desc }}</div>
            {%- endif -%}
          </a>
        </div>
        <div class="rc-timeline-nav__dot-cell" style="grid-column: {{ forloop.index }}">
          <a href="#{{ block.settings.anchor }}" class="rc-timeline-nav__dot" aria-label="{{ block.settings.eyebrow }}"></a>
        </div>
      {%- endfor -%}
    </div>
  </div>
</nav>

<!-- Service detail blocks -->
{%- for block in section.blocks -%}
  <section
    class="rc-service-detail"
    id="{{ block.settings.anchor | default: block.settings.title | downcase | replace: ' ', '-' }}"
    data-service-section="{{ block.settings.anchor | default: block.settings.title | downcase | replace: ' ', '-' }}"
  >
    <div class="rc-service-detail__inner">
      <div class="rc-service-detail__text">
        {%- if block.settings.eyebrow != blank -%}
          <span class="rc-service-detail__eyebrow">{{ block.settings.eyebrow }}</span>
        {%- endif -%}
        <h2 class="rc-service-detail__heading">{{ block.settings.title }}</h2>
        <p class="rc-service-detail__body">{{ block.settings.body }}</p>
        {%- if block.settings.list != blank -%}
          <ul class="rc-service-detail__list">
            {%- assign items = block.settings.list | newline_to_br | split: '<br />' -%}
            {%- for item in items -%}
              {%- if item != blank -%}<li>{{ item | strip }}</li>{%- endif -%}
            {%- endfor -%}
          </ul>
        {%- endif -%}
        {%- if block.settings.body_after != blank -%}
          <p class="rc-service-detail__body-after">{{ block.settings.body_after }}</p>
        {%- endif -%}
        {%- if block.settings.btn_label != blank -%}
          <a href="{{ block.settings.btn_url | default: '/pages/kontakta-oss' }}" class="rc-btn rc-btn--outline">{{ block.settings.btn_label }}</a>
        {%- endif -%}
      </div>
      <div class="rc-service-detail__image">
        {%- if block.settings.image != blank -%}
          <img src="{{ block.settings.image | image_url: width: 900 }}" alt="{{ block.settings.title }}" loading="lazy">
        {%- else -%}
          <div style="background:var(--rc-cream-alt);height:100%;min-height:260px;"></div>
        {%- endif -%}
      </div>
    </div>
  </section>
{%- endfor -%}

{% schema %}
{
  "name": "reCarpet Vad vi gör",
  "tag": "div",
  "class": "section",
  "settings": [],
  "blocks": [
    {
      "type": "service",
      "name": "Tjänst",
      "settings": [
        { "type": "text", "id": "anchor", "label": "Ankarlänk (t.ex. demontering)" },
        { "type": "text", "id": "eyebrow", "label": "Liten text" },
        { "type": "textarea", "id": "nav_desc", "label": "Kort beskrivning (visas i tidslinjen)" },
        { "type": "text", "id": "title", "label": "Titel" },
        { "type": "textarea", "id": "body", "label": "Brödtext" },
        { "type": "textarea", "id": "list", "label": "Lista (en rad per punkt)" },
        { "type": "textarea", "id": "body_after", "label": "Brödtext efter listan (valfri)" },
        { "type": "image_picker", "id": "image", "label": "Bild" },
        { "type": "text", "id": "btn_label", "label": "Knapptext" },
        { "type": "url", "id": "btn_url", "label": "Knapp länk" }
      ]
    }
  ],
  "presets": [
    {
      "name": "reCarpet Vad vi gör",
      "blocks": [
        {
          "type": "service",
          "settings": {
            "anchor": "inventering",
            "eyebrow": "Inventering",
            "nav_desc": "Vi kartlägger golvets skick och potential för återbruk.",
            "title": "Inspektion och bedömning av era mattor",
            "body": "Känns det ogranskat om ni funderar på att återbruka era textilplattor? ReCarpet guidar er genom materialåterbruksprocessen.",
            "btn_label": "Kontakta oss för inventering"
          }
        },
        {
          "type": "service",
          "settings": {
            "anchor": "demontering",
            "eyebrow": "Demontering",
            "nav_desc": "Effektiv demontering med fokus på återbruk.",
            "title": "Monterar bort och tvättar era befintliga mattor",
            "body": "Vi tillhandahåller professionell demonteringstjänst för era textilplattor.",
            "btn_label": "Kontakta oss för demontering"
          }
        },
        {
          "type": "service",
          "settings": {
            "anchor": "formedling",
            "eyebrow": "Förmedling",
            "nav_desc": "Vi hittar nya användare för dina gamla textilplattor.",
            "title": "Säljer återbrukade och nya textilplattor",
            "body": "Vi förmedlar återbrukade och nya kvalitetstextilplattor till konkurrenskraftiga priser.",
            "btn_label": "Se vårt sortiment"
          }
        },
        {
          "type": "service",
          "settings": {
            "anchor": "montering",
            "eyebrow": "Montering",
            "nav_desc": "Snabb och smidig installation av återbrukade eller nya golv.",
            "title": "Vi utför självklart montering av era nya mattor",
            "body": "Vi har samarbeten med professionella montörer från syd till norr.",
            "btn_label": "Kontakta oss för montering"
          }
        },
        {
          "type": "service",
          "settings": {
            "anchor": "service",
            "eyebrow": "Service & underhåll",
            "nav_desc": "Vi hjälper dig att hålla golven fräscha och i toppskick.",
            "title": "Vi underhåller era monterade mattor",
            "body": "Behöver du hjälp med fläckar, trasiga plattor eller lösa kanter?",
            "btn_label": "Kontakta oss"
          }
        },
        {
          "type": "service",
          "settings": {
            "anchor": "internt-aterbruk",
            "eyebrow": "Internt återbruk",
            "nav_desc": "Behåll golvet i fastigheten – vi gör det som nytt igen.",
            "title": "Återbruk av era tidigare mattor för er nästa hyresgäst",
            "body": "Behåll golvet i fastigheten! Vi uppgraderar befintliga plattor och förbereder dem för nästa hyresgäst.",
            "btn_label": "Läs mer"
          }
        }
      ]
    }
  ]
}
{% endschema %}
