<div class="rc-quote" style="background-color: {{ section.settings.background_color | default: 'var(--rc-cream, #F6F6F6)' }}">
  <div class="rc-quote__container">
    <div class="rc-quote__mark">"</div>
    <blockquote class="rc-quote__text">
      {{ section.settings.quote_text }}
    </blockquote>
    <div class="rc-quote__attribution">
      <div class="rc-quote__author">{{ section.settings.author_name }}</div>
      <div class="rc-quote__title">{{ section.settings.author_title }}</div>
    </div>
  </div>
</div>

<style>
  .rc-quote {
    width: 100%;
    padding: var(--rc-section-md) 1.25rem;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .rc-quote__container {
    max-width: 50rem;
    width: 100%;
    text-align: center;
  }

  .rc-quote__mark {
    font-size: clamp(3.75rem, 10vw, 7.5rem);
    line-height: 1;
    font-weight: 300;
    color: var(--rc-green);
    margin-bottom: 1.25rem;
    display: block;
    font-family: Georgia, serif;
  }

  .rc-quote__text {
    font-size: clamp(1.25rem, 2.5vw, 1.75rem);
    font-style: italic;
    font-weight: 500;
    line-height: 1.6;
    margin: 0;
    margin-bottom: 2.5rem;
    font-family: var(--rc-font);
    color: inherit;
  }

  .rc-quote__attribution {
    margin-top: 1.875rem;
  }

  .rc-quote__author {
    font-size: var(--rc-text-base);
    font-weight: 600;
    margin-bottom: 0.25rem;
    color: var(--rc-text);
    font-family: var(--rc-font);
  }

  .rc-quote__title {
    font-size: var(--rc-text-sm);
    font-weight: 400;
    color: var(--rc-text-muted);
    font-family: var(--rc-font);
  }

  /* Dark text on green background */
  .rc-quote[style*="--rc-green"],
  .rc-quote[style*="#1E4433"] {
    color: white;
  }

  .rc-quote[style*="--rc-green"] .rc-quote__mark,
  .rc-quote[style*="#1E4433"] .rc-quote__mark {
    color: white;
    opacity: 0.8;
  }

  .rc-quote[style*="--rc-green"] .rc-quote__author,
  .rc-quote[style*="#1E4433"] .rc-quote__author {
    color: white;
  }

  .rc-quote[style*="--rc-green"] .rc-quote__title,
  .rc-quote[style*="#1E4433"] .rc-quote__title {
    color: rgba(255, 255, 255, 0.8);
  }

  @media (max-width: 768px) {
    .rc-quote {
      padding: var(--rc-section-md) 1.25rem;
    }

    .rc-quote__mark {
      margin-bottom: 1rem;
    }

    .rc-quote__text {
      margin-bottom: 2rem;
    }
  }
</style>

{% schema %}
{
  "name": "RC Quote",
  "settings": [
    {
      "type": "textarea",
      "id": "quote_text",
      "label": "Quote Text",
      "placeholder": "Enter the testimonial or quote here"
    },
    {
      "type": "text",
      "id": "author_name",
      "label": "Author Name",
      "placeholder": "e.g., John Smith"
    },
    {
      "type": "text",
      "id": "author_title",
      "label": "Author Title",
      "placeholder": "e.g., CEO, Acme Corp"
    },
    {
      "type": "select",
      "id": "background_color",
      "label": "Background Color",
      "options": [
        {
          "value": "var(--rc-cream, #F6F6F6)",
          "label": "Cream"
        },
        {
          "value": "#FFFFFF",
          "label": "White"
        },
        {
          "value": "var(--rc-green, #1E4433)",
          "label": "Green"
        }
      ],
      "default": "var(--rc-cream, #F6F6F6)"
    }
  ],
  "presets": [
    {
      "name": "RC Quote"
    }
  ]
}
{% endschema %}
