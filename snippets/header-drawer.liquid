{% comment %}
  Renders a header drawer menu for mobile and desktop.

  Usage:
  {% render 'header-drawer' %}
{% endcomment %}

<header-drawer data-breakpoint="{% if section.settings.menu_type_desktop == 'drawer' %}desktop{% else %}tablet{% endif %}">
  <details id="Details-menu-drawer-container" class="menu-drawer-container">
    <summary
      class="header__icon header__icon--menu header__icon--summary link focus-inset"
      aria-label="{{ 'sections.header.menu' | t }}"
    >
      <span>
        {{- 'icon-hamburger.svg' | inline_asset_content -}}
        {{- 'icon-close.svg' | inline_asset_content -}}
      </span>
    </summary>
    <div id="menu-drawer" class="gradient menu-drawer motion-reduce color-{{ section.settings.menu_color_scheme }}">
      <div class="menu-drawer__inner-container">
        <div class="menu-drawer__navigation-container">
          <nav class="menu-drawer__navigation">
            <ul class="menu-drawer__menu has-submenu list-menu" role="list">
              {% assign showB2BNav = false %}
              
              {%- if showB2BNav == true -%}

                {%- if customer.metafields.sparklayer.authentication -%}
                  {% assign menu = linklists.b2b-menu | default: linklists[section.settings.menu] %}
                {%- else -%}
                  {% assign menu = linklists[section.settings.menu] %}
                {%- endif -%}

              {%- else -%}
  
                {% assign menu = linklists[section.settings.menu] %}

              {%- endif -%}

            {%- for link in menu.links -%}
                <li>
                  {%- if link.links != blank -%}
                    <details id="Details-menu-drawer-menu-item-{{ forloop.index }}">
                      <summary
                        id="HeaderDrawer-{{ link.handle }}"
                        class="menu-drawer__menu-item list-menu__item link link--text focus-inset{% if link.child_active %} menu-drawer__menu-item--active{% endif %}"
                      >
                        {{ link.title | escape }}
                        <span class="svg-wrapper">
                          {{- 'icon-arrow.svg' | inline_asset_content -}}
                        </span>
                        <span class="svg-wrapper">
                          {{- 'icon-caret.svg' | inline_asset_content -}}
                        </span>
                      </summary>
                      <div
                        id="link-{{ link.handle | escape }}"
                        class="menu-drawer__submenu has-submenu gradient motion-reduce"
                        tabindex="-1"
                      >
                        <div class="menu-drawer__inner-submenu">
                          <button class="menu-drawer__close-button link link--text focus-inset" aria-expanded="true">
                            <span class="svg-wrapper">
                              {{- 'icon-arrow.svg' | inline_asset_content -}}
                            </span>
                            {{ link.title | escape }}
                          </button>
                          <ul class="menu-drawer__menu list-menu" role="list" tabindex="-1">
                            {%- for childlink in link.links -%}
                              <li>
                                {%- if childlink.links == blank -%}
                                  <a
                                    id="HeaderDrawer-{{ link.handle }}-{{ childlink.handle }}"
                                    href="{{ childlink.url }}"
                                    class="menu-drawer__menu-item link link--text list-menu__item focus-inset{% if childlink.current %} menu-drawer__menu-item--active{% endif %}"
                                    {% if childlink.current %}
                                      aria-current="page"
                                    {% endif %}
                                  >
                                    {{ childlink.title | escape }}
                                  </a>
                                {%- else -%}
                                  <details id="Details-menu-drawer-{{ link.handle }}-{{ childlink.handle }}">
                                    <summary
                                      id="HeaderDrawer-{{ link.handle }}-{{ childlink.handle }}"
                                      class="menu-drawer__menu-item link link--text list-menu__item focus-inset"
                                    >
                                      {{ childlink.title | escape }}
                                      <span class="svg-wrapper">
                                        {{- 'icon-arrow.svg' | inline_asset_content -}}
                                      </span>
                                      <span class="svg-wrapper">
                                        {{- 'icon-caret.svg' | inline_asset_content -}}
                                      </span>
                                    </summary>
                                    <div
                                      id="childlink-{{ childlink.handle | escape }}"
                                      class="menu-drawer__submenu has-submenu gradient motion-reduce"
                                    >
                                      <button
                                        class="menu-drawer__close-button link link--text focus-inset"
                                        aria-expanded="true"
                                      >
                                        <span class="svg-wrapper">
                                          {{- 'icon-arrow.svg' | inline_asset_content -}}
                                        </span>
                                        {{ childlink.title | escape }}
                                      </button>
                                      <ul
                                        class="menu-drawer__menu list-menu"
                                        role="list"
                                        tabindex="-1"
                                      >
                                        {%- for grandchildlink in childlink.links -%}
                                          <li>
                                            <a
                                              id="HeaderDrawer-{{ link.handle }}-{{ childlink.handle }}-{{ grandchildlink.handle }}"
                                              href="{{ grandchildlink.url }}"
                                              class="menu-drawer__menu-item link link--text list-menu__item focus-inset{% if grandchildlink.current %} menu-drawer__menu-item--active{% endif %}"
                                              {% if grandchildlink.current %}
                                                aria-current="page"
                                              {% endif %}
                                            >
                                              {{ grandchildlink.title | escape }}
                                            </a>
                                          </li>
                                        {%- endfor -%}
                                      </ul>
                                    </div>
                                  </details>
                                {%- endif -%}
                              </li>
                            {%- endfor -%}
                          </ul>
                        </div>
                      </div>
                    </details>
                  {%- else -%}
                    <a
                      id="HeaderDrawer-{{ link.handle }}"
                      href="{{ link.url }}"
                      class="menu-drawer__menu-item list-menu__item link link--text focus-inset{% if link.current %} menu-drawer__menu-item--active{% endif %}"
                      {% if link.current %}
                        aria-current="page"
                      {% endif %}
                    >
                      {{ link.title | escape }}
                    </a>
                  {%- endif -%}
                </li>
              {%- endfor -%}
              {%- assign customer_tags_lower = customer.tags | join: ',' | downcase -%}
              {%- unless customer_tags_lower contains 'member' -%}
                <li>
                  <a
                    id="HeaderDrawer-b2b-login"
                    href="/pages/b2b-login"
                    class="menu-drawer__menu-item list-menu__item link link--text focus-inset"
                  >
                    B2B-inloggning
                  </a>
                </li>
              {%- endunless -%}
            </ul>
          </nav>
          <div class="menu-drawer__utility-links">
            {%- if shop.customer_accounts_enabled -%}
              <a
                href="{%- if customer -%}{{ routes.account_url }}{%- else -%}{{ routes.account_login_url }}{%- endif -%}"
                class="menu-drawer__account link focus-inset h5 medium-hide large-up-hide"
                rel="nofollow"
              >
                {%- if section.settings.enable_customer_avatar -%}
                  <account-icon>
                    {%- if customer and customer.has_avatar? -%}
                      {{ customer | avatar }}
                    {%- else -%}
                      <span class="svg-wrapper">
                        {{- 'icon-account.svg' | inline_asset_content -}}
                      </span>
                    {%- endif -%}
                  </account-icon>
                {%- else -%}
                  <span class="svg-wrapper">
                    {{- 'icon-account.svg' | inline_asset_content -}}
                  </span>
                {%- endif -%}
                {%- liquid
                  if customer
                    echo 'customer.account_fallback' | t
                  else
                    echo 'customer.log_in' | t
                  endif
                -%}
              </a>
            {%- endif -%}
            {%- comment -%} reCarpet: mobile collapsible locale switcher — trigger row with expandable options {%- endcomment -%}
            {%- if localization.available_countries and localization.available_countries.size > 1 -%}
              <div class="menu-drawer__localization header-localization" style="padding: 20px 30px;">
                {%- form 'localization', id: 'HeaderCountryMobileForm', class: 'localization-form' -%}
                  <input type="hidden" name="country_code" value="{{ localization.country.iso_code }}">
                  <input type="hidden" name="locale_code" value="{{ localization.language.iso_code }}">
                {%- endform -%}

                {%- capture flag_SE -%}<svg class="rc-flag" viewBox="0 0 20 15" xmlns="http://www.w3.org/2000/svg"><rect width="20" height="15" rx="2" fill="#006AA7"/><rect x="6" width="3" height="15" fill="#FECC02"/><rect y="6" width="20" height="3" fill="#FECC02"/></svg>{%- endcapture -%}
                {%- capture flag_NO -%}<svg class="rc-flag" viewBox="0 0 20 15" xmlns="http://www.w3.org/2000/svg"><rect width="20" height="15" rx="2" fill="#EF2B2D"/><rect x="6" width="3" height="15" fill="#002868"/><rect y="6" width="20" height="3" fill="#002868"/><rect x="6.5" width="2" height="15" fill="#fff"/><rect y="6.5" width="20" height="2" fill="#fff"/></svg>{%- endcapture -%}
                {%- capture flag_DK -%}<svg class="rc-flag" viewBox="0 0 20 15" xmlns="http://www.w3.org/2000/svg"><rect width="20" height="15" rx="2" fill="#C8102E"/><rect x="6" width="2.5" height="15" fill="#fff"/><rect y="6.25" width="20" height="2.5" fill="#fff"/></svg>{%- endcapture -%}
                {%- capture flag_FI -%}<svg class="rc-flag" viewBox="0 0 20 15" xmlns="http://www.w3.org/2000/svg"><rect width="20" height="15" rx="2" fill="#fff"/><rect x="6" width="3" height="15" fill="#003580"/><rect y="6" width="20" height="3" fill="#003580"/></svg>{%- endcapture -%}
                {%- capture flag_DE -%}<svg class="rc-flag" viewBox="0 0 20 15" xmlns="http://www.w3.org/2000/svg"><rect width="20" height="15" rx="2" fill="#FFCC00"/><rect width="20" height="5" rx="2" fill="#000"/><rect y="5" width="20" height="5" fill="#DD0000"/></svg>{%- endcapture -%}
                {%- capture flag_NL -%}<svg class="rc-flag" viewBox="0 0 20 15" xmlns="http://www.w3.org/2000/svg"><rect width="20" height="15" rx="2" fill="#21468B"/><rect width="20" height="5" rx="2" fill="#AE1C28"/><rect y="5" width="20" height="5" fill="#fff"/></svg>{%- endcapture -%}
                {%- capture flag_GB -%}<svg class="rc-flag" viewBox="0 0 20 15" xmlns="http://www.w3.org/2000/svg"><rect width="20" height="15" rx="2" fill="#012169"/><path d="M0,0 L20,15 M20,0 L0,15" stroke="#fff" stroke-width="2.5"/><path d="M0,0 L20,15 M20,0 L0,15" stroke="#C8102E" stroke-width="1.5"/><rect x="8" width="4" height="15" fill="#fff"/><rect y="5.5" width="20" height="4" fill="#fff"/><rect x="8.75" width="2.5" height="15" fill="#C8102E"/><rect y="6" width="20" height="3" fill="#C8102E"/></svg>{%- endcapture -%}

                {%- comment -%} Map current country to flag + name {%- endcomment -%}
                {%- case localization.country.iso_code -%}
                  {%- when 'SE' -%}{%- assign _cur_flag = flag_SE -%}{%- assign _cur_name = 'Sverige' -%}
                  {%- when 'NO' -%}{%- assign _cur_flag = flag_NO -%}{%- assign _cur_name = 'Norge' -%}
                  {%- when 'DK' -%}{%- assign _cur_flag = flag_DK -%}{%- assign _cur_name = 'Danmark' -%}
                  {%- when 'FI' -%}{%- assign _cur_flag = flag_FI -%}{%- assign _cur_name = 'Finland' -%}
                  {%- when 'DE' -%}{%- assign _cur_flag = flag_DE -%}{%- assign _cur_name = 'Deutschland' -%}
                  {%- when 'NL' -%}{%- assign _cur_flag = flag_NL -%}{%- assign _cur_name = 'Nederland' -%}
                  {%- when 'GB' -%}{%- assign _cur_flag = flag_GB -%}{%- assign _cur_name = 'United Kingdom' -%}
                  {%- else -%}{%- assign _cur_flag = '' -%}{%- assign _cur_name = localization.country.name -%}
                {%- endcase -%}

                <div class="rc-locale-mobile" data-rc-locale-mobile>
                  {%- comment -%} Trigger row: flag + country name + caret {%- endcomment -%}
                  <button type="button" class="rc-locale-mobile__trigger" aria-expanded="false">
                    <span class="rc-locale-mobile__flag">{{ _cur_flag }}</span>
                    <span class="rc-locale-mobile__trigger-label">{{ _cur_name }}</span>
                    <svg class="rc-locale-mobile__caret" width="10" height="6" viewBox="0 0 10 6" fill="none" xmlns="http://www.w3.org/2000/svg">
                      <path d="M1 1L5 5L9 1" stroke="currentColor" stroke-width="1.4" stroke-linecap="round" stroke-linejoin="round"/>
                    </svg>
                  </button>

                  {%- comment -%} Expandable options panel {%- endcomment -%}
                  <div class="rc-locale-mobile__panel" hidden>
                    {%- for country in localization.available_countries -%}
                      {%- if country.iso_code == localization.country.iso_code -%}{%- continue -%}{%- endif -%}
                      {%- case country.iso_code -%}
                        {%- when 'SE' -%}{%- assign _mflag = flag_SE -%}{%- assign _mname = 'Sverige' -%}{%- assign _mlang = 'sv' -%}
                        {%- when 'NO' -%}{%- assign _mflag = flag_NO -%}{%- assign _mname = 'Norge' -%}{%- assign _mlang = 'sv' -%}
                        {%- when 'DK' -%}{%- assign _mflag = flag_DK -%}{%- assign _mname = 'Danmark' -%}{%- assign _mlang = 'da' -%}
                        {%- when 'FI' -%}{%- assign _mflag = flag_FI -%}{%- assign _mname = 'Finland' -%}{%- assign _mlang = 'sv' -%}
                        {%- when 'DE' -%}{%- assign _mflag = flag_DE -%}{%- assign _mname = 'Deutschland' -%}{%- assign _mlang = 'de' -%}
                        {%- when 'NL' -%}{%- assign _mflag = flag_NL -%}{%- assign _mname = 'Nederland' -%}{%- assign _mlang = 'nl' -%}
                        {%- when 'GB' -%}{%- assign _mflag = flag_GB -%}{%- assign _mname = 'United Kingdom' -%}{%- assign _mlang = 'en' -%}
                        {%- else -%}{%- assign _mflag = '' -%}{%- assign _mname = country.name -%}{%- assign _mlang = 'sv' -%}
                      {%- endcase -%}
                      <button
                        type="button"
                        class="rc-locale-mobile__option"
                        data-country-code="{{ country.iso_code }}"
                        data-locale-code="{{ _mlang }}"
                      >
                        <span class="rc-locale-mobile__flag">{{ _mflag }}</span>
                        <span class="rc-locale-mobile__option-name">{{ _mname }}</span>
                      </button>
                    {%- endfor -%}
                  </div>
                </div>

                <style>
                  .rc-locale-mobile {
                    position: relative;
                  }

                  /* — Trigger row — */
                  .rc-locale-mobile__trigger {
                    display: flex;
                    align-items: center;
                    gap: 0.5rem;
                    width: 100%;
                    padding: 0;
                    background: none;
                    border: none;
                    cursor: pointer;
                    font-family: var(--rc-font, 'DM Sans', sans-serif);
                    font-size: 14px;
                    font-weight: 500;
                    color: var(--rc-off-black, #1a1a1a);
                    line-height: 1;
                  }
                  .rc-locale-mobile__trigger-label {
                    flex: 1;
                    text-align: left;
                  }
                  .rc-locale-mobile__caret {
                    width: 10px;
                    height: 6px;
                    flex-shrink: 0;
                    transition: transform 0.2s;
                  }
                  .rc-locale-mobile[data-open] .rc-locale-mobile__caret {
                    transform: rotate(180deg);
                  }
                  .rc-locale-mobile__flag {
                    display: inline-flex;
                    align-items: center;
                    line-height: 1;
                  }
                  .rc-locale-mobile__flag .rc-flag {
                    width: 20px;
                    height: 15px;
                    display: block;
                  }

                  /* — Expandable panel — */
                  .rc-locale-mobile__panel[hidden] {
                    display: none !important;
                  }
                  .rc-locale-mobile__panel {
                    display: flex;
                    flex-direction: column;
                    margin-top: 0.5rem;
                    padding-top: 0.5rem;
                    border-top: 1px solid var(--rc-beige, #e0ddd7);
                    animation: rcLocaleMobileIn 0.15s ease;
                  }
                  @keyframes rcLocaleMobileIn {
                    from { opacity: 0; transform: translateY(-4px); }
                    to   { opacity: 1; transform: translateY(0); }
                  }

                  .rc-locale-mobile__option {
                    display: flex;
                    align-items: center;
                    gap: 0.5rem;
                    width: 100%;
                    padding: 0.6rem 0;
                    background: none;
                    border: none;
                    cursor: pointer;
                    font-family: var(--rc-font, 'DM Sans', sans-serif);
                    font-size: 14px;
                    font-weight: 400;
                    color: var(--rc-off-black, #1a1a1a);
                    text-align: left;
                    transition: opacity 0.15s;
                  }
                  .rc-locale-mobile__option:hover {
                    opacity: 0.7;
                  }
                </style>

                <script>
                  (function() {
                    document.querySelectorAll('[data-rc-locale-mobile]').forEach(function(root) {
                      var trigger = root.querySelector('.rc-locale-mobile__trigger');
                      var panel   = root.querySelector('.rc-locale-mobile__panel');

                      /* Toggle panel */
                      trigger.addEventListener('click', function(e) {
                        e.stopPropagation();
                        var isOpen = root.hasAttribute('data-open');
                        if (isOpen) {
                          panel.hidden = true;
                          root.removeAttribute('data-open');
                          trigger.setAttribute('aria-expanded', 'false');
                        } else {
                          panel.hidden = false;
                          root.setAttribute('data-open', '');
                          trigger.setAttribute('aria-expanded', 'true');
                        }
                      });

                      /* Option click → submit form */
                      root.querySelectorAll('.rc-locale-mobile__option').forEach(function(opt) {
                        opt.addEventListener('click', function() {
                          var form = root.closest('.menu-drawer__localization').querySelector('form[action*="/localization"]');
                          if (!form) return;
                          var countryInput = form.querySelector('input[name="country_code"]');
                          var langInput    = form.querySelector('input[name="locale_code"]');
                          if (countryInput) countryInput.value = opt.getAttribute('data-country-code');
                          if (langInput)    langInput.value    = opt.getAttribute('data-locale-code');
                          form.submit();
                        });
                      });
                    });
                  })();
                </script>
              </div>
            {%- endif -%}
            <ul class="list list-social list-unstyled" role="list">
              {%- if settings.social_twitter_link != blank -%}
                <li class="list-social__item">
                  <a href="{{ settings.social_twitter_link }}" class="list-social__link link">
                    <span class="svg-wrapper">
                      {{- 'icon-twitter.svg' | inline_asset_content -}}
                    </span>
                    <span class="visually-hidden">{{ 'general.social.links.twitter' | t }}</span>
                  </a>
                </li>
              {%- endif -%}
              {%- if settings.social_facebook_link != blank -%}
                <li class="list-social__item">
                  <a href="{{ settings.social_facebook_link }}" class="list-social__link link">
                    <span class="svg-wrapper">
                      {{- 'icon-facebook.svg' | inline_asset_content -}}
                    </span>
                    <span class="visually-hidden">{{ 'general.social.links.facebook' | t }}</span>
                  </a>
                </li>
              {%- endif -%}
              {%- if settings.social_pinterest_link != blank -%}
                <li class="list-social__item">
                  <a href="{{ settings.social_pinterest_link }}" class="list-social__link link">
                    <span class="svg-wrapper">
                      {{- 'icon-pinterest.svg' | inline_asset_content -}}
                    </span>
                    <span class="visually-hidden">{{ 'general.social.links.pinterest' | t }}</span>
                  </a>
                </li>
              {%- endif -%}
              {%- if settings.social_instagram_link != blank -%}
                <li class="list-social__item">
                  <a href="{{ settings.social_instagram_link }}" class="list-social__link link">
                    <span class="svg-wrapper">
                      {{- 'icon-instagram.svg' | inline_asset_content -}}
                    </span>
                    <span class="visually-hidden">{{ 'general.social.links.instagram' | t }}</span>
                  </a>
                </li>
              {%- endif -%}
              {%- if settings.social_tiktok_link != blank -%}
                <li class="list-social__item">
                  <a href="{{ settings.social_tiktok_link }}" class="list-social__link link">
                    <span class="svg-wrapper">
                      {{- 'icon-tiktok.svg' | inline_asset_content -}}
                    </span>
                    <span class="visually-hidden">{{ 'general.social.links.tiktok' | t }}</span>
                  </a>
                </li>
              {%- endif -%}
              {%- if settings.social_tumblr_link != blank -%}
                <li class="list-social__item">
                  <a href="{{ settings.social_tumblr_link }}" class="list-social__link link">
                    <span class="svg-wrapper">
                      {{- 'icon-tumblr.svg' | inline_asset_content -}}
                    </span>
                    <span class="visually-hidden">{{ 'general.social.links.tumblr' | t }}</span>
                  </a>
                </li>
              {%- endif -%}
              {%- if settings.social_snapchat_link != blank -%}
                <li class="list-social__item">
                  <a href="{{ settings.social_snapchat_link }}" class="list-social__link link">
                    <span class="svg-wrapper">
                      {{- 'icon-snapchat.svg' | inline_asset_content -}}
                    </span>
                    <span class="visually-hidden">{{ 'general.social.links.snapchat' | t }}</span>
                  </a>
                </li>
              {%- endif -%}
              {%- if settings.social_youtube_link != blank -%}
                <li class="list-social__item">
                  <a href="{{ settings.social_youtube_link }}" class="list-social__link link">
                    <span class="svg-wrapper">
                      {{- 'icon-youtube.svg' | inline_asset_content -}}
                    </span>
                    <span class="visually-hidden">{{ 'general.social.links.youtube' | t }}</span>
                  </a>
                </li>
              {%- endif -%}
              {%- if settings.social_vimeo_link != blank -%}
                <li class="list-social__item">
                  <a href="{{ settings.social_vimeo_link }}" class="list-social__link link">
                    <span class="svg-wrapper">
                      {{- 'icon-vimeo.svg' | inline_asset_content -}}
                    </span>
                    <span class="visually-hidden">{{ 'general.social.links.vimeo' | t }}</span>
                  </a>
                </li>
              {%- endif -%}
            </ul>
          </div>
        </div>
      </div>
    </div>
  </details>
</header-drawer>
